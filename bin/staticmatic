#!/bin/sh

dir=`dirname $0`
. $dir/constants.sh

STATIC_MATIC=$GEM_HOME/bin/staticmatic
if [ -f $STATIC_MATIC ] ; then
  ./$dir/jruby $STATIC_MATIC $@
else
  echo "Cannot find staticmatic"
  exit 1
fi
