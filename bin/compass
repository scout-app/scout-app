#!/bin/sh

dir=`dirname $0`
. $dir/constants.sh

find $VENDOR -name compass | grep compass > /dev/null
if [ $? -eq 1 ] ; then 
  ./$dir/bundle
fi
./$dir/jruby $JRUBY_DIR/lib/ruby/gems/1.8/bin/compass $@