#!/bin/sh

dir=`dirname $0`
. $dir/constants.sh

find $VENDOR -name bundle | grep bundle > /dev/null
if [ $? -eq 1 ] ; then
  ./$dir/jruby -S gem install bundler
fi

pushd $dir/../ > /dev/null
./bin/jruby -S bundle $@
popd > /dev/null